generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model SharedInvoice {
  id        String   @id @default(cuid())
  payload   String
  createdAt DateTime @default(now())
}
