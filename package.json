{
  "name": "invoice-mvp",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=@invoice/web",
    "build:web": "npm run build --workspace=@invoice/web",
    "lint:web": "npm run lint --workspace=@invoice/web"
  }
}
